apiVersion: v1
kind: ConfigMap
metadata:
  name: agents-graphql-api-configmap
  labels:
    {{- include "agents-graphql-api.labels" . | nindent 4 }}
data:
  config.yaml: |
  {{- with .Values.config }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
